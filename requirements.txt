# ─────────────────────────────────────────────────────────────────────────
# The two notebooks run in different environments.
#
# 01_subtitle_extraction.ipynb : WhisperX requires an older torch (1.13.1).
#   The notebook cells create a dedicated virtualenv (whisper-env) and
#   install into it, so nothing from that stage is listed here (it would
#   conflict with the system torch).
#
# 02_fake_news_classification.ipynb : recent transformers / datasets.
#   → the package list below.
# ─────────────────────────────────────────────────────────────────────────

# Installed by notebook 01 in a separate cell (listed here for reference only)
# yt-dlp
# ffmpeg-python
# whisperx @ git+https://github.com/m-bain/whisperx.git@v2.0.1

# Notebook 02 — train.py uses `eval_strategy` (transformers 4.41+; previously
# `evaluation_strategy`), so the lower bound is 4.41.
torch>=2.0
transformers>=4.41,<4.46
datasets>=2.14,<3.0
accelerate>=0.25
evaluate>=0.4
sentencepiece
scikit-learn
pandas
numpy
